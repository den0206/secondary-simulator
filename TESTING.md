# 動作確認指示書

## 前提条件

### 必須
- Node.js 18+ がインストールされていること
- VSCode または Cursor がインストールされていること

### iOS Simulator を使用する場合
- macOS であること
- Xcode がインストールされていること
- `xcrun simctl` コマンドが使用可能であること

### Android Emulator を使用する場合
- Android SDK がインストールされていること
- `adb` コマンドが PATH に含まれていること

---

## セットアップ手順

### 1. 依存関係のインストール

```bash
cd /Users/yuukisakai/Free-Projects/secondary-simulator
npm install
```

### 2. コンパイル

```bash
npm run compile
```

コンパイルが成功すると `out/` ディレクトリにファイルが生成されます。

### 3. 開発モードで起動

**VSCode の場合:**
1. このプロジェクトを VSCode で開く
2. `F5` を押して Extension Development Host を起動

**Cursor の場合:**
1. このプロジェクトを Cursor で開く
2. `Run and Debug` パネルを開く
3. `Run Extension` を選択して実行

---

## 動作確認項目

### 1. 拡張機能の読み込み確認

**手順:**
1. Extension Development Host が起動したら、Activity Bar を確認
2. 「Simulator」アイコン (スマートフォンアイコン) が表示されていることを確認

**期待結果:**
- Activity Bar に Simulator アイコンが表示される
- クリックすると「Device Preview」パネルが開く

---

### 2. iOS デバイス一覧の取得

**前提:** macOS + Xcode がインストールされていること

**手順:**
1. Simulator パネルを開く
2. Platform セレクトボックスで「iOS」を選択
3. Device セレクトボックスをクリック

**期待結果:**
- 利用可能な iOS シミュレータの一覧が表示される
- 起動中のデバイスには "(Booted)" などの状態が表示されない
- シャットダウン中のデバイスには "(Shutdown)" が表示される

**確認コマンド:**
```bash
xcrun simctl list devices -j
```

---

### 3. Android デバイス一覧の取得

**前提:** Android SDK がインストールされ、エミュレータが起動していること

**手順:**
1. Simulator パネルを開く
2. Platform セレクトボックスで「Android」を選択
3. Device セレクトボックスをクリック

**期待結果:**
- 接続されている Android デバイス/エミュレータの一覧が表示される

**確認コマンド:**
```bash
adb devices -l
```

---

### 4. スクリーンキャプチャの開始 (iOS)

**前提:** iOS Simulator が起動していること

**手順:**
1. iOS シミュレータを起動する
   ```bash
   open -a Simulator
   ```
2. Simulator パネルで「iOS」を選択
3. 「Refresh」ボタンをクリックしてデバイス一覧を更新
4. 起動中のデバイスを選択

**期待結果:**
- シミュレータの画面がパネルに表示される
- ステータスバーに FPS と遅延時間が表示される
- 画面が定期的に更新される (デフォルト 500ms)

---

### 5. スクリーンキャプチャの開始 (Android)

**前提:** Android エミュレータが起動していること

**手順:**
1. Android エミュレータを起動する
2. Simulator パネルで「Android」を選択
3. 「Refresh」ボタンをクリックしてデバイス一覧を更新
4. 起動中のデバイスを選択

**期待結果:**
- エミュレータの画面がパネルに表示される
- ステータスバーに FPS と遅延時間が表示される

---

### 6. タップ操作

**手順:**
1. キャプチャが開始されている状態で、表示された画面をクリック
2. シミュレータ/エミュレータの反応を確認

**期待結果:**
- クリックした位置がシミュレータ/エミュレータに送信される
- 対応するアプリ/UIがタップに反応する

---

### 7. ホームボタン

**手順:**
1. キャプチャが開始されている状態で「Home」ボタンをクリック

**期待結果:**
- iOS: ホーム画面に戻る
- Android: ホーム画面に戻る

---

### 8. 戻るボタン (Android のみ)

**手順:**
1. Android エミュレータでアプリを開いた状態で「Back」ボタンをクリック

**期待結果:**
- 前の画面に戻る
- iOS では警告がログに出力される (iOS には戻るボタンがないため)

---

### 9. スクリーンショット保存

**手順:**
1. キャプチャが開始されている状態で「Save」ボタンをクリック
2. 保存ダイアログが表示されたら、保存先を指定

**期待結果:**
- PNG ファイルが指定した場所に保存される
- 保存完了のメッセージが表示される

---

### 10. デバイス一覧の更新

**手順:**
1. 新しいシミュレータ/エミュレータを起動
2. 「Refresh」ボタンをクリック

**期待結果:**
- デバイス一覧が更新される
- 新しく起動したデバイスが表示される

---

### 11. 設定の変更

**手順:**
1. VSCode の設定を開く (`Cmd+,` or `Ctrl+,`)
2. 「Secondary Simulator」で検索
3. 以下の設定を変更:
   - `refreshInterval`: 1000 (1秒)
   - `imageQuality`: 50 (低品質)
   - `maxWidth`: 250 (小さいサイズ)

**期待結果:**
- リフレッシュ間隔が変更される (FPS が下がる)
- 画像品質が下がる (ファイルサイズ減少)
- 表示サイズが小さくなる

---

### 12. エラーハンドリング

**手順:**
1. シミュレータ/エミュレータを起動せずにデバイスを選択
2. 起動していないデバイスを選択

**期待結果:**
- 「Device is not running」などのエラーメッセージが表示される
- アプリがクラッシュしない

---

### 13. メモリリーク確認

**手順:**
1. キャプチャを開始
2. 5分以上放置
3. Activity Monitor (macOS) または Task Manager (Windows) で Extension Host のメモリ使用量を確認

**期待結果:**
- メモリ使用量が大きく増加しない
- 一定範囲内で安定している

---

### 14. パネルの非表示/表示

**手順:**
1. キャプチャが開始されている状態でパネルを閉じる
2. 再度パネルを開く

**期待結果:**
- パネルを閉じるとキャプチャが停止する
- パネルを再度開いても自動的にキャプチャは開始しない (デバイスを再選択する必要がある)

---

## トラブルシューティング

### コンパイルエラーが発生する

```bash
npm run clean
npm install
npm run compile
```

### iOS Simulator が検出されない

```bash
# Xcode Command Line Tools を確認
xcode-select -p

# シミュレータ一覧を確認
xcrun simctl list devices
```

### Android Emulator が検出されない

```bash
# adb の場所を確認
which adb

# デバイス一覧を確認
adb devices
```

### スクリーンショットが取得できない

Output パネルで「Secondary Simulator」を選択してログを確認してください。

---

## ログの確認方法

1. VSCode の Output パネルを開く (`View` → `Output`)
2. ドロップダウンから「Secondary Simulator」を選択
3. ログメッセージを確認

---

## 報告事項

問題が発生した場合は、以下の情報を含めて報告してください:

1. OS とバージョン
2. VSCode/Cursor のバージョン
3. Node.js のバージョン
4. Xcode のバージョン (iOS の場合)
5. Android SDK のバージョン (Android の場合)
6. 再現手順
7. エラーメッセージ (ログから)
8. 期待した動作と実際の動作
