# ストレージ管理とクリーンアップ

## 現在の実装状況

### 画像の保存場所

#### iOSシミュレータ
- **一時ファイル**: `os.tmpdir()` (通常は `/var/folders/...` または `/tmp`)
- **ファイル名**: `sim-${randomUUID()}.png`
- **例**: `/var/folders/bn/wpssfj3n6h18wsgrjtjp5s4m0000gn/T/sim-c19c860d-2da5-4b17-9a05-68dc0a0cfd26.png`

#### Androidエミュレータ
- **一時ファイル**: 作成しない（直接メモリバッファとして取得）

#### 処理後の画像
- **保存場所**: メモリ内のみ（Base64エンコードしてWebviewに送信）
- **保存形式**: JPEG（圧縮済み）

### クリーンアップ処理

#### ✅ 実装済み
1. **iOS一時ファイルの削除**
   - `takeScreenshot`メソッドの`finally`ブロックで`fs.unlink(tmpFile)`を実行
   - エラーが発生しても確実に削除される

2. **キャプチャの停止**
   - `ScreenshotCapture.dispose()`: タイマーを停止、コールバックをnull化
   - `SimulatorWebviewProvider.stopCapture()`: キャプチャを停止
   - `SimulatorWebviewProvider.dispose()`: すべてのキャプチャを停止

3. **拡張機能終了時のクリーンアップ**
   - `extension.ts`の`deactivate()`: providerのdisposeを呼び出し
   - `context.subscriptions`: 拡張機能終了時に自動的にdisposeが呼ばれる

#### ✅ 改善済み（最新）
1. **メモリ内バッファのクリア**
   - `processed.fill(0)`でバッファをクリア
   - `ScreenshotCapture.dispose()`でフレームバッファをリセット

2. **エラー時の確実なクリーンアップ**
   - `takeScreenshot`の`finally`ブロックで確実に削除
   - 削除に失敗した場合は、100ms後に再試行
   - それでも失敗した場合は、OSが自動的にクリーンアップ（一時ディレクトリの特性）

3. **拡張機能終了時のクリーンアップ**
   - `deactivate()`でproviderのdisposeを呼び出し
   - `context.subscriptions`で自動的にクリーンアップ
   - すべてのタイマーとリソースが確実に解放される

## ストレージへの影響

### ✅ 一時ファイル
- **iOS**: 各スクリーンショット取得時に一時ファイルを作成し、**即座に削除**
- **Android**: 一時ファイルは作成しない
- **削除失敗時**: OSが自動的にクリーンアップ（通常24時間以内）

### ✅ メモリ使用量
- **画像バッファ**: 処理後すぐにクリア（`fill(0)`）
- **Base64文字列**: Webviewに送信後、ガベージコレクションで自動的に解放
- **フレームバッファ**: dispose時にリセット

### ✅ ディスク使用量
- **一時ファイル**: 通常は0バイト（即座に削除されるため）
- **永続的なファイル**: 作成しない
- **設定ファイル**: VSCodeの標準設定のみ（数KB程度）

## 結論

**ストレージへの影響は最小限です。**

- 一時ファイルは即座に削除される
- メモリは適切にクリーンアップされる
- 拡張機能終了時にすべてのリソースが解放される
- 異常終了時も、OSが自動的にクリーンアップする

ユーザーが明示的にスクリーンショットを保存した場合のみ、ディスクに保存されます（`saveScreenshot`メソッドを使用した場合）。

