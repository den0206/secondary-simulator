{
  "name": "secondary-simulator",
  "displayName": "Secondary Simulator",
  "description": "Display iOS/Android simulators in the sidebar with real-time screen capture",
  "version": "0.1.0",
  "publisher": "secondary-simulator",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Debuggers",
    "Other"
  ],
  "keywords": [
    "ios",
    "android",
    "simulator",
    "emulator",
    "preview",
    "mobile"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "simulator-container",
          "title": "Simulator",
          "icon": "$(device-mobile)"
        }
      ]
    },
    "views": {
      "simulator-container": [
        {
          "type": "webview",
          "id": "simulatorView",
          "name": "Device Preview"
        }
      ]
    },
    "commands": [
      {
        "command": "simulator.selectDevice",
        "title": "Simulator: Select Device"
      },
      {
        "command": "simulator.startCapture",
        "title": "Simulator: Start Capture"
      },
      {
        "command": "simulator.stopCapture",
        "title": "Simulator: Stop Capture"
      },
      {
        "command": "simulator.takeScreenshot",
        "title": "Simulator: Save Screenshot"
      },
      {
        "command": "simulator.home",
        "title": "Simulator: Press Home"
      },
      {
        "command": "simulator.back",
        "title": "Simulator: Press Back"
      },
      {
        "command": "simulator.refresh",
        "title": "Simulator: Refresh Device List"
      }
    ],
    "configuration": {
      "title": "Secondary Simulator",
      "properties": {
        "secondarySimulator.maxWidth": {
          "type": "number",
          "default": 420,
          "description": "Maximum display width in pixels (for MJPEG streaming)"
        },
        "secondarySimulator.experimentalH264": {
          "type": "boolean",
          "default": false,
          "description": "Enable experimental H.264 streaming (requires adb/simctl and WebCodecs). Falls back to JPEG if not available."
        },
        "secondarySimulator.tapThreshold": {
          "type": "number",
          "default": 10,
          "minimum": 1,
          "maximum": 100,
          "description": "Tap判定に使用する移動距離の閾値（ピクセル）。"
        },
        "secondarySimulator.swipeThreshold": {
          "type": "number",
          "default": 30,
          "minimum": 5,
          "maximum": 200,
          "description": "スワイプ判定に使用する移動距離の閾値（ピクセル）。"
        },
        "secondarySimulator.showGestureOverlay": {
          "type": "boolean",
          "default": true,
          "description": "タップ／スワイプ時に視覚フィードバックを表示する。"
        },
        "secondarySimulator.longPressDuration": {
          "type": "number",
          "default": 600,
          "minimum": 200,
          "maximum": 2000,
          "description": "ロングタップ判定までの時間（ミリ秒）。"
        },
        "secondarySimulator.useMjpegStreaming": {
          "type": "boolean",
          "default": false,
          "description": "MJPEGストリーミングを使用する（mobilecliが必要）。実機デバイスもサポートされます。"
        },
        "secondarySimulator.useMobileCli": {
          "type": "boolean",
          "default": false,
          "description": "mobilecliを使用してデバイス操作を統一する（実機デバイスもサポート）。useMjpegStreamingが有効な場合、自動的に有効になります。"
        }
      }
    },
    "keybindings": [
      {
        "command": "simulator.takeScreenshot",
        "key": "ctrl+shift+s",
        "mac": "cmd+shift+s",
        "when": "view == simulatorView"
      },
      {
        "command": "simulator.refresh",
        "key": "ctrl+shift+r",
        "mac": "cmd+shift+r",
        "when": "view == simulatorView"
      },
      {
        "command": "simulator.home",
        "key": "ctrl+shift+h",
        "mac": "cmd+shift+h",
        "when": "view == simulatorView"
      },
      {
        "command": "simulator.back",
        "key": "ctrl+shift+b",
        "mac": "cmd+shift+b",
        "when": "view == simulatorView"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "clean": "rm -rf out"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/vscode": "^1.80.0",
    "@typescript-eslint/eslint-plugin": "^8.47.0",
    "@typescript-eslint/parser": "^8.47.0",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@mobilenext/mobilecli": "^0.0.42",
    "sharp": "^0.34.5"
  }
}
